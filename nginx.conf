# Nginx configuration for AMOS Engines

events {
    worker_connections 1024;
}

http {
    upstream amos_engines {
        server amos-engines:8000;
    }

    server {
        listen 80;
        server_name localhost;

        location / {
            proxy_pass http://amos_engines;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /health {
            proxy_pass http://amos_engines/health;
            access_log off;
        }

        location /metrics {
            proxy_pass http://amos_engines:9090/metrics;
            allow 127.0.0.1;
            deny all;
        }
    }
}
