# AMOS Brain Cognitive OS - Requirements
# Python 3.10+ required
#
# DEPRECATED: This file is kept for backward compatibility only.
# All dependencies are now defined in pyproject.toml with proper extras.
#
# USE THESE COMMANDS INSTEAD:
#   pip install -e .                           # Core only
#   pip install -e ".[server]"                # + FastAPI/Uvicorn
#   pip install -e ".[database]"              # + SQLAlchemy/PostgreSQL
#   pip install -e ".[security]"               # + Auth/JWT/Rate limiting
#   pip install -e ".[events]"                # + Redis/Celery/Kafka
#   pip install -e ".[ml]"                    # + PyTorch/Transformers/OpenAI
#   pip install -e ".[graphql]"               # + Strawberry GraphQL
#   pip install -e ".[observability]"         # + OpenTelemetry
#   pip install -e ".[search]"               # + Elasticsearch/Neo4j
#   pip install -e ".[reports]"              # + Excel/PDF generation
#   pip install -e ".[dev]"                  # + Testing/Linting tools
#   pip install -e ".[all]"                   # Everything
#
# For Docker/production, the Dockerfile uses:
#   pip install ".[server,database,security,events,ml]"
#
# NOTE: This file will be removed in a future release.
#       Migrate to pyproject.toml-based installation.

# Kept for CI compatibility during transition
-e .
