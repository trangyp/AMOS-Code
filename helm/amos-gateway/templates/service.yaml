apiVersion: v1
kind: Service
metadata:
  name: {{ include "amos-gateway.fullname" . }}
  labels:
    {{- include "amos-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: 8000
      targetPort: http
      protocol: TCP
      name: metrics
  selector:
    {{- include "amos-gateway.selectorLabels" . | nindent 4 }}
